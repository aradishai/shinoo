[phases.setup]
nixPkgs = ["nodejs_24", "python3", "gcc", "gnumake"]

[phases.build]
cmds = ["npm ci", "npx prisma generate", "npx prisma db push --accept-data-loss", "npm run build"]

[start]
cmd = "node scripts/setup-db.js && npm start"
