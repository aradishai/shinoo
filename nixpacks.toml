[phases.setup]
nixPkgs = ["nodejs_24", "python3", "gcc", "gnumake"]

[phases.build]
cmds = ["npm ci", "npx prisma generate", "npm run build"]

[start]
cmd = "npx prisma db push --accept-data-loss && node scripts/setup-db.js && npm start"
