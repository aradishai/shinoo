{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "node scripts/setup-db.js && node_modules/.bin/next start -p ${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE"
  }
}
